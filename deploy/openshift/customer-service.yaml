apiVersion: v1
kind: ConfigMap
metadata:
  name: customer-service-config
data:
  CUSTOMER_DB_URL: jdbc:postgresql://pgtest.platform.svc.cluster.local:5432/testdb
  CUSTOMER_DB_USER: testuser
  KAFKA_BOOTSTRAP_SERVERS: kafka:9092
  KEYCLOAK_ISSUER_URI: https://keycloak-platform.apps-crc.testing/realms/master
  CUSTOMER_EVENTS_ENABLED: "true"
  CUSTOMER_EVENTS_TOPIC: customer.events
  SERVER_PORT: "8443"
  SERVER_SSL_ENABLED: "true"
---
apiVersion: v1
kind: Secret
metadata:
  name: customer-service-secret
stringData:
  CUSTOMER_DB_PASSWORD: testpass
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: customer-service
  template:
    metadata:
      labels:
        app: customer-service
    spec:
      containers:
        - name: customer-service
          image: customer-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8443
          envFrom:
            - configMapRef:
                name: customer-service-config
            - secretRef:
                name: customer-service-secret
          env:
            - name: JAVA_TOOL_OPTIONS
              value: "-Djavax.net.ssl.trustStore=/etc/ssl/openshift/truststore.jks -Djavax.net.ssl.trustStorePassword=changeit"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8443
              scheme: HTTPS
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8443
              scheme: HTTPS
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          volumeMounts:
            - name: truststore
              mountPath: /etc/ssl/openshift
              readOnly: true
            - name: service-tls
              mountPath: /etc/tls
              readOnly: true
      volumes:
        - name: truststore
          secret:
            secretName: platform-truststore
        - name: service-tls
          secret:
            secretName: customer-service-tls
---
apiVersion: v1
kind: Service
metadata:
  name: customer-service
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: customer-service-tls
spec:
  selector:
    app: customer-service
  ports:
    - port: 8443
      targetPort: 8443
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: customer-service
spec:
  host: customer-service-platform.apps-crc.testing
  to:
    kind: Service
    name: customer-service
  port:
    targetPort: 8443
  tls:
    termination: reencrypt
    insecureEdgeTerminationPolicy: Redirect
